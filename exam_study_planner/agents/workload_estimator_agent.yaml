# yaml-language-server: $schema=https://raw.githubusercontent.com/google/adk-python/refs/heads/main/src/google/adk/agents/config_schemas/AgentConfig.json
agent_class: LlmAgent
name: workload_estimator_agent
model: gemini-2.0-flash
description: Estimates study time per topic based on ingested documents.
instruction: |
  Estimate time per topic using the ingested summaries and topic lists.
  Store per-topic time estimates in session state for scheduling.
tools:
  - name: study_planner.tools.estimate_topic_load
